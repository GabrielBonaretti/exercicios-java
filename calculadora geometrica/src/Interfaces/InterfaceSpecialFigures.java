package Interfaces;

import java.util.Scanner;

// This interface represents a contract for special geometric figures in three dimensions.
public interface InterfaceSpecialFigures {

    // This method calculates and returns the volume of the special geometric figure.
    public double volume();

    // This method is used to set the attributes or properties of the special geometric figure
    public void setAtributes(Scanner sc);
}
